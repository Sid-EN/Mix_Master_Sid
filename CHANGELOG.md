# 版本紀錄 Changelog

本檔案記錄 MixMaster 的所有重要變更。

格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本編號依循 [語意化版本 2.0.0](https://semver.org/lang/zh-TW/)。

## 版本編號規則

版本號格式為 `主版本.次版本.修訂號`（例：`1.2.3`）：

| 層級 | 何時遞增 | 說明 |
|---|---|---|
| **主版本**（大更版） | 不相容的變更 | API 契約、資料格式或行為改變，既有整合需要調整才能運作 |
| **次版本**（小更版） | 新增功能 | 向下相容地加入功能或端點，既有用法不受影響 |
| **修訂號** | 修正缺陷 | 向下相容的錯誤修正、效能改善、文件與內部重構 |

預發行版本以 `-alpha`、`-beta`、`-rc.1` 標示（例：`2.0.0-beta`）。

變更分類：`新增`（Added）、`變更`（Changed）、`修正`（Fixed）、
`移除`（Removed）、`安全性`（Security）、`棄用`（Deprecated）。

---

## [未發布]

尚無變更。

---

## [2.9.0] — 2026-09-07

十二項新功能全數完成。

### 新增

- **購物清單**（/shopping-list）。從配方頁或派對規劃加入缺少的材料，
  同一材料合併為一筆並累加需求量，可勾選、清除已買、匯出純文字。
- **成本與庫存**。酒櫃可為每項材料填入容量、售價與剩餘量（皆選填）；
  配方頁據此顯示單杯成本、以現有庫存還能調幾杯，以及你缺哪幾項。
- **派對規劃**（/party）。選好酒單與杯數，彙總材料總用量、
  對照庫存算出缺口與要買幾瓶，並估算花費。
- **公開配方目錄**（/discover）。可依最新、評分、名稱排序並搜尋；
  與權杖分享明確區分——後者是「知道網址的人看得到」，
  前者是「所有人都找得到」。
- **追蹤創作者**與動態牆。只顯示追蹤對象的公開作品。
- **QR 分享**。分享對話框同時提供連結、QR 圖與 SVG 下載。
  QR 編碼器為自行實作，對照另一個獨立實作的基準驗證。
- **SEO 與社群分享預覽**。每頁各自的標題與描述、Open Graph、
  建置期產生的 1200×630 預覽圖、配方頁的 schema.org 結構化資料、
  sitemap.xml（96 個網址）與 robots.txt。
- **口味統計**（/dashboard）。依實際收藏與紀錄推導風味偏向、
  常用材料、偏好手法，並與全站平均比較。
- **完整離線**。可主動下載全部配方，之後沒有網路也能查閱
  未曾造訪的頁面；離線搜尋亦可用。
- **配方內容雙語**。51 道配方的步驟、裝飾、筆記、典故與餐搭
  全部譯為英文（約 650 段文字），未翻譯處回退中文並說明。

### 變更

- **容錯搜尋**已於 2.8.0 加入，此版補上靜態版的離線索引快取。
- 我的酒櫃改為卡片內可展開的庫存明細，並修正巢狀按鈕的結構。
- 儀表板新增「我的口味統計」與「離線閱讀」兩個區塊。

### 修正

- **無障礙**：以 axe-core 掃描全站 52 頁，導入時 118 處以上問題，
  現為 0（深色與淺色主題皆是）。包含對比不足、未命名的表單元件、
  標題層級斷層、只靠顏色區分的連結，並補上跳至主要內容的連結、
  可見的焦點樣式與「減少動態效果」支援。
- E2E 測試已達 168 項，會超過後端預設的 100/分鐘限流；
  CI 的 E2E 工作改為停用限流，與 pytest 的作法一致。

---

## [2.8.0] — 2026-09-07

### 新增

- **容錯搜尋與相關性排序**（新功能 5）。先前是單純的子字串比對，
  打錯一個字母就完全沒有結果，順序也等同資料檔順序。
  - 依詞長允許 0～2 個編輯距離（三字以下不容錯，否則 gin／sin／tin 互相命中）
  - 名稱／標籤／材料／描述各有權重，命中方式（完全相同／前綴／包含／模糊）亦然
  - 所有查詢詞都必須命中（AND 語意），多打一個詞會收斂而非擴大結果
  - 回傳 `exact` 旗標，介面在只有近似結果時明說原因
  - 搜尋視窗支援 ↑↓ 與 Enter，並補上 combobox／listbox 可存取屬性
- **靜態版也能搜尋**。改在瀏覽器端比對建置期產生的精簡索引
  （36 KB，第一次搜尋才載入），不必把 136 KB 的配方檔打進 bundle。
- `tests/fixtures/fuzzy_parity.json`：前後端兩份實作的共同對照基準，
  任一側改動而另一側沒跟上時測試即失敗。

### 修正

- **GitHub Pages 上 PWA 與離線功能完全失效**。Service Worker 註冊、
  manifest、圖示與 sw.js 內的快取路徑都是寫死的斜線開頭路徑，
  在 `/<repo>/` 之下一律 404，Service Worker 根本沒註冊成功。
- **靜態版中文搜尋失效**。JavaScript 的 `\w` 只涵蓋 ASCII，
  中日文字元會被當成分隔符號而切不出任何詞。由對照基準檔當場抓到。
- **乾淨建置時配方與備料詳情頁回 500**。兩頁匯出了 `generateStaticParams`，
  Next 視為靜態頁，但頁面以 `cache: 'no-store'` 取資料，
  渲染時從靜態轉為動態。本機殘留的 `.next` 會用舊的預產生頁面掩蓋此問題。
- **CI 自 2.4.0 起持續失敗**，先前未察覺：
  - 推播測試依賴未進版控的本機 `backend/.env` 金鑰（CI 上整組路由回 503）
  - `frontend/data/` 由 `backend/data` 產生且已 gitignore，全新簽出時型別檢查失敗
  - E2E 因前面的工作失敗而一直被跳過，從未真正執行過

### 變更

- CI 的 E2E 工作即時產生一組 VAPID 金鑰（僅該次執行有效），
  並保留前後端伺服器輸出且於結束時印出——先前後端一旦出錯完全看不到原因。
- 首頁配方摘要在靜態版改讀建置期產生的 `recipes-summary.json`，
  欄位對齊後端的 `SUMMARY_FIELDS`。

---

## [2.7.0] — 2026-09-07

### 新增

- **GitHub Pages 公開展示版**。同一份程式碼可產出兩種形態，
  以建置期環境變數 `NEXT_PUBLIC_STATIC_MODE` 區分。
  - `npm run build:static` 產生靜態站台（123 個 HTML 頁）
  - `.github/workflows/pages.yml` 於推送至 main 時自動部署
  - 51 道經典配方與 18 種備料以 `generateStaticParams` 預先產生完整詳情頁
- `docs/DEPLOYMENT.md`：兩種部署形態的完整步驟，
  含未來以 Vercel + Railway + Neon 部署完整版的設定與注意事項。

### 設計取捨

- **靜態版直接讀取 repo 內的 JSON，而非於建置期呼叫 API**。
  經典配方與材料本就是靜態資產，如此建置不需要執行中的後端，
  CI 也不必額外啟動服務。
- **需要伺服器的功能改顯示說明並可導向完整版**，而非靜默失敗。
  讓功能壞掉不講會使人以為是 bug；明講「這是展示版」才有用。
- `/shared/[token]` 與 `/recipes/mine/[slug]` 於匯出時暫時移出——
  兩者依賴使用者資料，建置期無從得知有哪些頁面。匯出後自動還原。

### 修正

- 靜態模式的判斷原本插在元件的 hooks 之前，違反 React 的 hooks 規則
  （每次渲染須以相同順序呼叫），ESLint 報 42 項 rules-of-hooks 錯誤。
  改以包裝元件決定渲染哪一個，hooks 仍在單一元件內順序固定。

### 移除

- `backend/data/user_recipes.json`：使用者配方已於 2.0.0 遷移至資料庫，
  此檔為遺留產物且已無程式碼讀取。

---

## [2.6.1] — 2026-09-04

### 新增

- **前端單元測試由 39 項增至 133 項**，覆蓋率 7.4% → 30%。
  依風險排序而非追求覆蓋率數字，優先涵蓋出錯代價最高的模組：

  | 模組 | 覆蓋率 | 為何優先 |
  |---|---|---|
  | `lib/sync.ts` | 96% | 曾因邏輯缺陷造成實際的資料遺失 |
  | `lib/recipeCache.ts` | 100% | 新增的請求去重邏輯 |
  | `BackupSettings` | 92% | 外部檔案進入系統的入口 |
  | `FavoritesContext` | 91% | 使用者個人資料，遺失無從復原 |
  | `ProgressContext` | 87% | 長期累積的成果 |
  | `AuthContext` | 86% | 權杖處理錯誤會導致誤登出 |

### 修正

- `jest.setup.js` 補上 `Blob.prototype.text` 的 polyfill。
  jsdom 尚未實作此 API，但它在所有現代瀏覽器皆可用——
  補於測試設定而非改動正式程式碼。

### 已知限制（新增記錄）

- **SyncAgent 的 localStorage 攔截機制無法於 jsdom 驗證**。
  jsdom 將 Storage 的屬性賦值一律視為「儲存同名項目」，因此覆寫
  `localStorage.setItem` 不會生效；實測真實 Chromium 則正常
  （賦值建立自有屬性、攔截器確實被呼叫、寫入照常保存）。
  該機制改由 E2E 涵蓋，單元測試僅驗證 jsdom 下確實可觀察的部分。

---

## [2.6.0] — 2026-09-04

效能優化。以實測的傳輸量為依據，而非憑檔案大小推測。

### 變更

- **圖表函式庫改為動態載入**。recharts 原以靜態方式匯入，
  約 340 KB 併入 `/tools/cost`、`/compare`、`/engine` 的初始 bundle，
  但圖表位於畫面下方且非首屏必要。
- **合併首頁的重複請求**。首頁三個元件各自抓取完整配方清單
  （`limit=100` 一次、`limit=200` 兩次），同一份 121 KB 的 JSON
  被下載三次共 363 KB。新增 `lib/recipeCache.ts` 共用同一個請求。
- **新增 `fields=summary` 欄位投影**。列表只顯示名稱、手法與等第，
  卻傳回含 story、steps、tips 的完整配方——51 道即達 121 KB，
  其中列表實際用到的僅 7 KB。預設維持 `full` 以免影響既有整合。

### 實測改善（正式建置、每頁全新 context）

| 路由 | 總傳輸量前 | 後 | 減少 |
|---|---|---|---|
| `/` | 1443 KB | 789 KB | −45% |
| `/tools/cost` | 1126 KB | 782 KB | −31% |
| `/compare` | 1092 KB | 773 KB | −29% |
| `/engine` | 1072 KB | 773 KB | −28% |
| `/recipes` | 934 KB | 808 KB | −13% |

配方清單 API：121 KB → 12 KB（`fields=summary`，−90%）。

### 未處理

每頁仍有約 550 KB 的共用 JS，主要為 React 19 與 Next 16 的執行期，
屬框架本身的下限，進一步壓縮的效益不明確，故未動。

---

## [2.5.0] — 2026-09-04

### 新增

- **安裝為應用程式的提示**：攔截 `beforeinstallprompt` 改以本站樣式呈現；
  iOS Safari 不支援該事件，改為引導手動「加入主畫面」。
  已安裝或使用者關閉過提示時不再顯示。
- **推播通知**：採 Web Push 協定搭配 VAPID，直接送至瀏覽器廠商的推送服務，
  不需 FCM 之類的外部帳號。
  - `GET /api/v1/push/public-key`、`POST/DELETE /api/v1/push/subscribe`
  - `POST /api/v1/push/test` 對自己發送測試通知
  - Service Worker 加入 `push` 與 `notificationclick` 處理
  - 帳號頁新增推播開關
- 新增 `push_subscriptions` 資料表；同一使用者可有多個裝置的訂閱。

### 設計取捨

- 未設定 VAPID 金鑰時整組功能自動停用，公鑰端點明確回報 `enabled: false`，
  而非給出無效的金鑰讓前端訂閱失敗。
- 推送服務回報 404／410 代表訂閱已不存在（使用者清除瀏覽器資料或撤銷權限），
  據此清除該筆訂閱以免持續重試；其他錯誤視為暫時性，保留訂閱。
- 同一 endpoint 再次訂閱時更新其歸屬帳號——同一瀏覽器換帳號登入是常見情境。
- 點擊通知時優先聚焦已開啟的分頁，而非每次都開新視窗。

### 已知限制

- **實際的推播送達未能於本機驗證**：headless Chromium 不連線至瀏覽器廠商的
  推送服務（回報 "push service not available"），無痕環境亦不支援 Push API。
  訂閱管理與發送邏輯已以模擬方式測試（17 項），
  Service Worker 的處理器亦確認存在，但端到端送達需於實際瀏覽器環境驗證。

---

## [2.4.0] — 2026-09-04

### 新增

- **資料匯出與匯入**：`GET /api/v1/backup/export` 匯出同步資料與自建配方，
  `POST /api/v1/backup/import` 還原。帳號頁新增備份區塊，
  可下載 JSON 檔或選擇檔案還原。
- **配方列印**：分享頁新增列印按鈕，並加入列印樣式——
  深色底在紙上既難讀又耗墨，列印時改為白底黑字並隱藏導覽與互動元件，
  外部連結網址則標示於文字後供紙本讀者參照。

### 設計取捨

- 匯入為**合併式**：覆寫同名項目、以新代號新增配方，但不刪除備份中沒有的
  既有資料。匯入應是增添而非抹除，否則誤匯入舊備份會靜默清空現有內容。
- 匯入逐項驗證：同步項目僅接受白名單內的鍵，配方則走與建立時相同的驗證，
  分數一律由引擎重算而不採信備份中的數值。並設有檔案大小與配方數上限。
- 匯出不含分享權杖：分享狀態不應隨備份轉移，匯入他處後由該帳號自行決定。
- 備份帶有格式版本，高於系統支援者予以拒絕。

### 修正

- 匯入時的配方代號衝突檢查原本僅比對本人的配方，但 slug 為全域唯一，
  匯入他人備份時會撞上對方既有的代號而失敗；改為比對全域。

---

## [2.3.0] — 2026-09-04

### 新增

- **個人化推薦**：`GET /api/v1/engine/recommendations`。
  以使用者收藏過的配方合成 15 維口味輪廓，再推薦風味相近者。
  首頁新增「依你的收藏推薦」區塊，標示依據的收藏數與主導風味，
  並顯示各配方的風味相似度與手邊材料的齊全度。

### 設計取捨

- 尚無收藏時回傳評分最高的經典配方，並以 `basis: "popular"` 明確標示依據；
  前端在此情況下不顯示個人化區塊——沒有依據卻標示為個人化推薦，
  只是把熱門清單換個名字。
- 收藏的評分納入權重（1 星 0.2、5 星 2.0）：低分收藏代表不喜歡，
  不應與高分等量拉高偏好輪廓。
- 手邊材料齊全的配方僅略為加分（0.15），不喧賓奪主——
  推薦的核心仍是口味相近。
- 已收藏的配方不再出現於推薦。
- 權杖無效時退回熱門推薦而非回報錯誤：推薦屬輔助資訊，
  不應因登入狀態異常而讓首頁區塊失效。

### 變更

- 首頁既有的偏好推薦區塊維持不變，兩者依據不同（手動設定的風味偏好
  vs 實際收藏行為）；新區塊改名為「依你的收藏推薦」以免與其標題相同。
- `optional_user` 抽取至 `backend/auth.py` 供多處共用。

---

## [2.2.0] — 2026-09-04

### 新增

- **社群互動**：已公開分享的配方可由他人評分與留言。
  - `GET/PUT/DELETE /api/v1/community/{token}/ratings`
  - `GET/POST /api/v1/community/{token}/comments`、`DELETE .../{id}`
  - 公開分享頁加入評分與留言區
- 新增 `recipe_ratings` 與 `recipe_comments` 資料表。

### 設計取捨

- 互動對象限定為已公開分享的配方，並一律以分享權杖定位——
  未公開的配方屬私人內容，不應讓他人評分或留言。撤銷分享後即無法再互動。
- 評分獨立於 sync 的 favorites：後者是使用者自己的私人筆記，
  此處是可跨使用者彙總的公開評價，兩者用途不同故分開儲存。
- 每位使用者對同一配方僅能有一筆評分（以唯一索引保證），
  且不可為自己的配方評分。
- 留言僅回傳顯示名稱，不外洩電子郵件。
- 留言刪除權限：留言者可刪自己的，配方擁有者可刪自己配方下的任一則。

### 修正

- 全為空白的留言原本可通過 `min_length` 檢查，去除空白後將存為空留言；
  改為在去除空白後再驗證。

---

## [2.1.0] — 2026-09-04

### 新增

- **配方版本歷史**：每次更新配方前自動保存當下內容，可檢視各版本並回溯。
  - `GET /api/v1/recipes/{id}/versions`：版本清單
  - `GET /api/v1/recipes/{id}/versions/{n}`：檢視單一版本
  - `POST /api/v1/recipes/{id}/versions/{n}/restore`：回溯
  - `/recipes/mine/{slug}/versions` 版本歷史頁面
- 新增 `user_recipe_versions` 資料表，以 (recipe_id, version) 為唯一索引。

### 設計取捨

- 僅保留最近 20 版：長期編輯若不設上限，歷史會無限成長。
  超出時汰除最舊的版本。
- 回溯本身也保存為一個新版本，使回溯動作同樣可以還原——
  否則使用者誤按回溯後將無從挽回。
- 版本僅擁有者可存取；他人一律回 404 而非 403，與配方本身的行為一致。
- 刪除配方時經外鍵串聯一併移除其版本歷史。

---

## [2.0.0] — 2026-09-04

使用者配方改為隸屬帳號，並新增公開分享功能。

### ⚠ 不相容變更

- **建立、修改、刪除使用者配方now需要登入**。先前這些端點無需認證，
  且配方沒有擁有者概念——**任何未登入者都能刪除他人的配方**（已實測確認）。
  現配方綁定帳號，僅擁有者可修改。
- **使用者配方改由 `GET /api/v1/recipes/mine` 取得**，不再出現於
  `GET /api/v1/recipes` 或 `/recipes/{id}`。後兩者僅提供經典配方。
- **使用者配方改存資料庫**，不再使用 `backend/data/user_recipes.json`。
  該檔為開發用途且未納入版控，既有內容不會自動移轉。

### 新增

- **配方分享**：`POST /api/v1/recipes/{id}/share` 產生公開連結，
  `DELETE` 撤銷，`GET /api/v1/recipes/shared/{token}` 無需登入即可檢視。
  另新增 `/shared/[token]` 公開頁面與 `/recipes/mine` 管理頁。
- `user_recipes` 資料表：slug 與分享權杖獨立成欄以建立唯一索引，
  配方內容則以 JSONB 整包儲存，因其欄位會隨功能演進。

### 安全性

- 分享採用不可猜測的隨機權杖，而非直接以配方代號作為公開網址——
  後者一旦外流便無從收回。配方預設不公開。
- 存取他人配方一律回 404 而非 403，避免洩漏該配方是否存在。
- 重複分享同一配方會沿用既有權杖，不會使先前發出的連結失效。

---

## [1.4.0] — 2026-09-04

補齊帳號系統的密碼管理。先前使用者無法變更密碼，忘記密碼也無從救回。

### 新增

- **變更密碼**：`POST /api/v1/auth/change-password`，需驗證目前密碼。
- **忘記密碼與重設**：`POST /api/v1/auth/forgot-password` 與
  `/auth/reset-password`，以及 `/account/reset` 重設頁面。
  權杖有效期 1 小時且僅能使用一次。
- **更新帳號資料**：`PATCH /api/v1/auth/me`，可變更顯示名稱。
- 帳號頁新增「帳號設定」區塊，登入頁新增「忘記密碼」入口。

### 安全性

- **變更或重設密碼會使先前簽發的所有權杖失效**。以使用者上的
  `token_version` 計數實作，權杖內帶版本號，比對不符即拒絕。
  不採用簽發時間比對——JWT 的 `iat` 僅有秒精度，同一秒內簽發與撤銷
  無從區分，該秒內舊權杖仍可使用。
- 重設權杖僅以 SHA-256 雜湊形式存放，資料庫外洩亦無法據以重設他人密碼；
  權杖絕不出現在 API 回應中（否則任何人都能重設他人密碼），
  目前輸出至伺服器日誌，正式環境應改接電子郵件服務。
- 申請重設一律回傳 202 且訊息相同，不透露該信箱是否已註冊，
  避免此端點淪為帳號探測工具。
- 變更密碼會一併撤銷所有尚未使用的重設權杖。

### 修正

- 登出後表單停留在註冊分頁，導致既有帳號登入時回報「此電子郵件已註冊」；
  現於登出時重置為登入模式。

---

## [1.3.0] — 2026-09-04

升級前端框架至現行支援版本，解除全部正式相依的安全公告。

### 變更

- **Next.js 14.2.35 → 16.3.4、React 18.3.1 → 19.2.8**。
  14.x 分支已無修補版，剩餘 5 項 high 公告涵蓋整個 14.x，其中
  「rewrites 的 HTTP request smuggling」與本專案直接相關（以 rewrites
  代理後端）。升級後**正式相依的弱點歸零**。
- 動態路由改為 `await params`。Next 15 起 `params` 為 Promise，
  `/recipes/[slug]` 與 `/prep/[slug]` 需解構後使用。
- ESLint 8 → 9 並遷移至 flat config（`eslint.config.mjs`）。
  Next 16 移除了 `next lint`，改為直接執行 `eslint .`。
- recharts 2.12.4 → 3.10.1（前者不支援 React 19）。
- postcss → 8.5.23，解除 4 項 high 公告。
- 9 個檔案的站內 `<a>` 改用 `next/link` 的 `<Link>`，恢復客戶端導覽。

### 修正

- `ConfettiEffect` 於 `useMemo` 中呼叫 `Math.random()`，屬渲染期不純運算，
  伺服器與瀏覽器會得到不同結果而造成水合不一致；改於 effect 中產生。

### 移除

- 13 個宣告但從未被引用的前端相依：5 個 @radix-ui 元件、
  @hookform/resolvers、@tanstack/react-query、clsx、lucide-react、
  react-hook-form、tailwind-merge、zod、zustand。
  其中 Radix 舊版正是阻擋 React 19 升級的原因。

---

## [1.2.1] — 2026-09-04

### 修正

- **登入後的變更不會同步，且會在下次登入時遺失**（嚴重）。
  同步原本只在登入時執行一次，之後於本機所做的變更永遠不會上傳；
  再次登入時伺服器的舊資料還會覆寫本機，使用者新增的酒櫃材料就此消失。
  新增 SyncAgent 於登入期間持續將變更推送至伺服器（1.5 秒 debounce，
  並在分頁隱藏時盡力送出），並記錄每項資料「最後成功上傳的內容」，
  據以判斷本機是否有未同步的變更。
- **兩條同步路徑競態**。帳號頁與 SyncAgent 各自執行同步時，
  pullAll 取得伺服器舊值後，另一條路徑的推送會把資料標記為已同步，
  isDirty 隨即變為 false，最後以舊快照覆寫本機。
  同步改由 SyncAgent 單一負責，帳號頁僅監聽結果事件。
- 登出時清除同步狀態，避免下一位登入者沿用前一位的已同步紀錄。

### 安全性

- **正式環境沿用預設 SECRET_KEY 時拒絕啟動**。以預設密鑰簽發的 JWT
  任何人都能偽造，原本此疏漏會在部署後靜默生效而無人察覺。
- **Next.js 14.2.3 → 14.2.35**，解除包含 Cache Poisoning（critical）
  在內的多項公告。前端正式相依的 critical 弱點歸零。

### 移除

- `backend/models/ingredient.py`：未被任何程式碼引用的死碼。

### 變更

- CI 的 GitHub Actions 升版（checkout v5、setup-python v6、setup-node v5、
  upload-artifact v5），原版本以 Node 20 為目標而遭 runner 標記棄用。

---

## [1.2.0] — 2026-09-03

新增帳號系統與跨裝置同步。既有的未登入使用方式完全不變，
資料仍存於瀏覽器，因此為向下相容的次版本更新。

### 新增

- **帳號系統**：`POST /api/v1/auth/register`、`POST /api/v1/auth/login`
  與 `GET /api/v1/auth/me`。密碼以 bcrypt 雜湊儲存，存取權杖為 HS256
  簽章的 JWT，效期 7 天。
- **跨裝置同步**：`/api/v1/sync` 系列端點，可同步收藏與評分、我的酒櫃、
  學習進度、風味偏好、測驗紀錄與調酒人格共六項。佈景主題與語言屬單一
  裝置的顯示偏好，刻意不納入同步。
- **資料庫層**：以 SQLAlchemy 定義 `users` 與 `user_data`，並以 Alembic
  管理結構遷移。使用者資料在前端本就是形狀各異的 JSON 且會隨功能演進，
  故以 `(user_id, key)` 為索引的 JSONB 文件儲存，而非為每項功能各建一表。
- **帳號頁面**：`/account` 提供註冊、登入、登出與同步狀態顯示；
  導覽列新增帳號入口。
- **登入時的資料合併**：伺服器已有的項目以伺服器為準，伺服器沒有但本機
  有的項目則上傳，確保既有使用者的酒櫃與收藏不會在首次登入時消失。
- 測試：帳號 25 項、同步 24 項、端對端 9 項，涵蓋使用者隔離、
  偽造與過期權杖、密碼雜湊儲存等安全性行為。
- CI 的後端與 E2E job 加入 PostgreSQL 服務容器，並於 E2E 前套用遷移。

### 變更

- 設定檔的 `.env` 路徑改為絕對路徑。原本以相對路徑指定，而應用實際由
  專案根目錄啟動，導致 `backend/.env` 從未被讀取。
- `bcrypt` 由 4.1.2 降至 4.0.1。passlib 1.7.4 無法讀取 bcrypt 4.1 的
  版本資訊，會在每次雜湊時輸出錯誤堆疊；功能雖不受影響但日誌雜訊嚴重。
- ruff 設定豁免 FastAPI 的相依項宣告（`Depends` 等），該寫法為框架標準
  用法，B008 對此為誤報；其餘真正的可變預設值仍會被攔截。

### 安全性

- 登入失敗時，帳號不存在與密碼錯誤回傳相同訊息，避免洩漏哪些信箱已註冊。
- 同步端點僅以權杖識別身分，不接受 `user_id` 之類的參數，
  並以白名單限制可同步的項目與單筆 256 KB 上限。
- 刪除帳號時，其個人資料經由外鍵串聯一併移除。

---

## [1.1.0] — 2026-09-03

本次同時修復多項使用者可見的缺陷、建立完整測試體系與 CI，並新增使用者
自建配方等功能。因新增功能皆向下相容，故為次版本更新。

### 新增

- **使用者自建配方**：新增 `POST`／`PUT`／`DELETE /api/v1/recipes` 端點與
  `/recipes/new` 建立表單。配方分數與等第一律由引擎以與經典配方相同的模型
  計算，不接受前端指定；經典配方為唯讀資產，不可經此修改或刪除。
- **我的酒櫃 × 替代品建議**：「差一點就能做」的配方會依使用者手邊材料
  推薦可用的替代品。
- **API 速率限制**：接上 slowapi，全域與配方生成端點採不同限制，
  並回傳 `X-RateLimit-*` 標頭。以 `RATE_LIMIT_ENABLED=false` 可關閉。
- **測試體系**：資料完整性、引擎單元、API 整合、前端單元與端對端共五層，
  後端 172 項、前端 39 項、E2E 69 項。
- **CI**：GitHub Actions 三個 job——後端（ruff／mypy／pytest）、
  前端（型別檢查／lint／單元測試／建置）、E2E（Playwright）。
- **程式碼品質把關**：導入 ruff 與 mypy 設定並納入 CI。

### 變更

- **平衡評分模型改為家族導向**：依烈酒基調、長飲、酸味短飲分別以該家族的
  可接受區間評分，區間取自 51 道經典配方的實測分布。
  與人工評分的平均誤差由 54.2 分降至 4.4 分。
- **替代品排序改以相似度為準**：原公式將「互補度」納入排序，但互補度衡量
  的是兩者搭配協調，與可互相取代恰好相反。改為「風味相似度 × 類別相容度」
  並設 0.5 門檻，找不到合適替代品時回傳空清單。
- **資料載入集中化並加入快取**：新增 `backend/data_store.py`，資料檔僅於
  啟動時載入一次。吞吐量由 557 req/s 提升至 870 req/s（+56%），
  併發 50 時 p50 由 89.4 ms 降至 57.1 ms。
- **後端位址改為可設定**：`next.config.js` 的 rewrites 與前端 API 呼叫
  改由 `API_BASE_URL`／`NEXT_PUBLIC_API_URL` 決定，不再寫死 localhost。
- **相依清單整理**：移除未使用的 scipy（103 MB），測試與品質工具移至
  `requirements-dev.txt`。
- 專案結構：真實專案由 `PracticeAICCD_Sid/` 上移至倉庫根目錄。

### 修正

- **配方詳情頁材料顯示佔位字串**：51 道配方共 195 筆材料原本全數顯示
  「材料」，因 API 僅回傳 slug 而未附名稱。
- **批次換算顯示原始 slug**：批次結果顯示 `tanqueray-gin` 而非
  「添加利倫敦乾琴酒」。
- **平衡模型將烈酒基調家族評為 0 分**：Negroni、Manhattan、Old Fashioned
  等本就無酸味的配方被除零防護誤判，51 道經典配方中有 25 道（49%）得 0 分。
- **17 個材料 slug 無法解析**（32 處參照）：13 種為命名不一致，
  另補上 4 種確實缺漏的材料。
- **配方列表 API 回傳 422**：`limit` 上限為 100，但前端 5 處以 `limit=200`
  請求，導致首頁等頁面無法載入配方資料。
- **`/recipes/[slug]` 軟 404**：對不存在的配方回傳 HTTP 200。
  成因為 `loading.tsx` 的 Suspense 邊界使狀態碼於 `notFound()` 前送出；
  改以 route group 將 `loading.tsx` 限定於列表頁。
- **離線時無法瀏覽已造訪的配方**：Service Worker 未快取導覽回應，
  先前看似可用實為瀏覽器 HTTP 快取所致。
- **風味計算未換算單位**：10 leaves 薄荷被當成 10 oz、
  2 dashes 苦精被當成 2 oz（高估 32 倍）。
- **酸度以對數 pH 線性計算**：通寧水（pH 3.0）被視為與萊姆汁（pH 2.2）
  同級的酸味來源，改以氫離子濃度比值計算。
- `getRecommendedPairings` 未遵守 `limit` 參數。
- 調製手法清單原本散落三處且互不一致，現以 `models.recipe.RecipeMethod`
  為唯一權威。

### 安全性

- 修正無效的 CORS 組合：`allow_origins=["*"]` 與 `allow_credentials=True`
  併用時瀏覽器會直接拒絕，改為萬用字元來源時自動關閉 credentials。

---

## [1.0.0-alpha] — 2026-04-10

初始版本。包含智慧風味引擎、51 道經典配方、調酒學院、批次換算與
多語系介面等既有功能。
