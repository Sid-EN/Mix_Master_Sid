# ============================================================
# MixMaster — 開發與測試依賴
# ============================================================
# 安裝指令：pip install -r requirements.txt -r requirements-dev.txt
# 正式環境不需安裝本清單。
# ============================================================

# ── 測試 (Testing) ──────────────────────────────────────────
pytest==8.1.1                 # 測試框架
pytest-asyncio==0.23.6        # 非同步測試支援
pytest-cov==5.0.0             # 測試覆蓋率
factory-boy==3.3.0            # 測試資料工廠

# ── 程式碼品質 (Code Quality) ───────────────────────────────
ruff==0.3.5                   # 高速 Linter（取代 flake8 + isort）
black==24.3.0                 # 程式碼格式化
mypy==1.9.0                   # 靜態型別檢查
